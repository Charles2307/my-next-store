function SiteFooter() {
    return (
        <footer className="mt-20 border-t border-subtle">
            <div className="shell flex h-16 items-center justify-between text-sm text-muted">
                <span>React Store</span>
                <span>Built with Next.js</span>
            </div>
        </footer>
    );
}

export default SiteFooter;
